# Queue UI screenshots

Placeholder PNGs for CI/docs until manual captures are added.

| File | Description |
|------|-------------|
| queue-default.png | Default queue row styling with theme tokens |
| queue-hover.png | Hover/focus-visible state on queue item |
| queue-drag.png | Drag reorder with .dragging / .drag-over classes |

Capture manually: `npm run dev`, import tracks, exercise keyboard (Arrow/Home/End), drag handle reorder, switch theme via header dropdown.

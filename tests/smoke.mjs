import fs from 'fs';import path from 'path';import{fileURLToPath}from'url';import{runThemeSwitchSmoke}from'./smoke/harness.mjs';const root=path.join(path.dirname(fileURLToPath(import.meta.url)),'..');const reportsDir=path.join(root,'tests','reports');let failures=0;const logLines=[];function log(m){logLines.push(m);console.log(m);}function check(n,fn){try{fn();log('PASS:'+n);}catch(e){failures++;log('FAIL:'+n+':'+e.message);}}async function checkAsync(n,fn){try{await fn();log('PASS:'+n);}catch(e){failures++;log('FAIL:'+n+':'+e.message);}}async function main(){check('smoke-script',()=>{const p=JSON.parse(fs.readFileSync(path.join(root,'package.json'),'utf8'));if(!p.scripts?.smoke?.includes('scripts/smoke.mjs'))throw new Error('smoke must use scripts/smoke.mjs');if(!fs.existsSync(path.join(root,'scripts/smoke.mjs')))throw new Error('scripts/smoke.mjs missing');});check('theme-unit-files',()=>{for(const f of['tests/theme-validate.test.mjs','tests/theme-shades.test.mjs','tests/theme-persist.test.mjs']){if(!fs.existsSync(path.join(root,f)))throw new Error(f);}});check('vitest-runtime-files',()=>{for(const f of['tests/smoke.vitest.mjs','tests/theme-queue-scrollbar.vitest.mjs']){if(!fs.existsSync(path.join(root,f)))throw new Error(f);}});check('queue-modern',()=>{const m=fs.readFileSync(path.join(root,'src/main.js'),'utf8');const c=fs.readFileSync(path.join(root,'src/styles.css'),'utf8');if(!m.includes('queue-item')||!m.includes('setupQueueKeyboard'))throw new Error('queue js');if(!c.includes('--tuner-scrollbar-size')||!c.includes('scrollbar-thumb:active'))throw new Error('queue css');});await checkAsync('theme-switch-runtime',async()=>{const r=await runThemeSwitchSmoke();if(!r.thumbBefore||!r.thumbAfter)throw new Error('theme switch failed');});log('TOTAL_FAILURES:'+failures);fs.mkdirSync(reportsDir,{recursive:true});fs.writeFileSync(path.join(reportsDir,'smoke.log'),logLines.join('\n'),'utf8');process.exit(failures?1:0);}main();
